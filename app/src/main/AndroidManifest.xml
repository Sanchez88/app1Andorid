<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.ronald.app1andorid" >

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".actLogin"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <service android:name="com.servicios.rfs.clServicio" />

        <activity
            android:name=".actRegistro"
            android:label="@string/app_name" >
        </activity>
        <activity
            android:name=".act_amigos"
            android:label="@string/app_name" >
        </activity>
        <activity
            android:name=".actDetalle"
            android:label="@string/title_activity_act_detalle" >
        </activity>
        <activity
            android:name=".act_chat"
            android:label="@string/title_activity_act_chat" >
        </activity>
    </application>

</manifest>
